# Daily Changelog - 2026-03-18

## Documentation
- Rollback dilakukan untuk seluruh folder dan file yang sebelumnya dipindahkan dari lokasi aslinya.
- `docs/core/`, `docs/TCT--Laravel--Legacy-main/`, dan `docs/STRUKTUR DOCS.md` dipulihkan ke jalur semula.
- Prinsip dokumentasi diperjelas: arsip adalah lapisan tambahan non-destruktif, bukan pengganti struktur lama.

## Rules Clarified
- Perapihan docs tidak boleh mengubah nama folder atau file yang sudah ada.
- Jika current situation memerlukan struktur yang lebih jelas, solusinya adalah menambah folder atau dokumen baru tanpa merusak susunan lama.

## Product Documentation
- Ditambahkan audit mendalam UI/UX di `docs/01-audits/overall/2026-03-18-ui-ux-audit.md`.
- Ditambahkan arah masa depan produk di `docs/03-architecture/product/future-ux-direction.md`.
- Ditambahkan roadmap praktis `UI/UX V1 -> V2` di `docs/02-roadmap/ui-ux-v1-to-v2-roadmap.md`.

## UI Implementation
- Landing page `/` dirombak agar lebih selaras dengan arah produk sebagai spiritual companion yang tenang dan terfokus.
- Shortcut primer pada Today tidak lagi mendorong `channels`; kini diarahkan ke `paths` agar entry flow lebih sesuai dengan arsitektur produk inti.
- Route legacy yang harus dipensiunkan kini diarahkan via redirect:
  - `/library` -> `/versehub/id`
  - `/visitors` -> `/community`
  - `/gate-updates` -> `/today`
  - `/reflections/:path*` -> `/community?intent=reflection`
- CTA refleksi pada detail `paths` tidak lagi menuju route `reflections` lama, dan sekarang langsung handoff ke `community` dengan context query (`intent`, `ref`, `text`).
- Shortcut core lama yang masih menonjolkan `channels` juga dirapikan ke `paths` pada komponen reusable agar tidak ada drift CTA di surface aktif.
- Halaman `/channels` tetap berjalan, tetapi kini diberi penanda transisi yang secara eksplisit mengarahkan user ke pilar inti (`/paths` dan `/community`).
- Tiga route legacy page-level (`/library`, `/visitors`, `/gate-updates`) disederhanakan menjadi redirect server-side ringan agar surface UI lawas tidak tampil lagi.
- `docs/09-handover/current-status.md` diperbarui dengan snapshot frontend product terkini.
- `docs/09-handover/next-actions.md` diisi ulang menjadi urutan eksekusi frontend praktis (V1) yang fokus implementasi.
- Sweep final CTA/label legacy pada surface aktif diselesaikan:
  - Label VerseHub `Library/Perpustakaan` diubah menjadi `Choose Book/Pilih Kitab`.
  - Label detail Paths `Kembali ke Perpustakaan` diubah menjadi `Kembali ke Paths`.
  - Scan source memastikan tidak ada CTA aktif yang mengarah ke route IA legacy (`/channels`, `/library`, `/visitors`, `/gate-updates`, `/reflections`) di luar namespace route legacy/transisi.
