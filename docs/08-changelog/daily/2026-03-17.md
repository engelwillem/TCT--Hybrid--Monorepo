# Daily Changelog - 2026-03-17

## Documentation
- Audit menyeluruh folder `docs/` untuk menilai progres web aktif, kualitas sumber kebenaran, dan next step yang paling tepat.
- `docs/README.md` diperjelas agar tim bisa membedakan dokumen aktif, historis, dan snapshot legacy.
- Material historis dipusatkan ke `docs/archive/` agar current web situation tidak bercampur dengan arsip.
- `docs/09-handover/current-status.md` diselaraskan ke realitas saat ini: domain inti lokal banyak yang sudah matang, sedangkan blocker utama sudah bergeser ke readiness production.
- `docs/09-handover/next-actions.md` dan `docs/09-handover/open-blockers.md` dirapikan agar hanya memuat urutan kerja dan blocker yang masih aktif.
- `docs/06-testing/parity/local-vs-production-checklist.md` dibersihkan dari drift status yang masih menandai `Community` sebagai blocker lokal walau dokumen domain terbaru menunjukkan `PASS/CLOSED`.
- `docs/06-testing/parity/env-diff-log.md` ditambah entri cookie/CORS parity agar konfigurasi session lintas origin lebih eksplisit.

## Audit Output
- Ditambahkan `docs/01-audits/overall/2026-03-17-docs-audit.md` sebagai ringkasan audit, posisi progres proyek, dan rekomendasi langkah berikutnya.

## Repo / Parity / UI
- Laravel bootstrap kini mengaktifkan `statefulApi()` untuk alur Sanctum SPA.
- Ditambahkan `backend-api/config/sanctum.php` serta dokumentasi env parity di `backend-api/.env.example`.
- Host publik default diselaraskan ke `https://www.thechoosentalks.org` pada metadata layout dan community share URL fallback.
- Halaman `/today` dipoles lagi dengan intro panel kontekstual per state dan atmosfer visual yang lebih sejalan dengan Dawn theme.
- Verifikasi live menunjukkan `www.thechoosentalks.org`, `/today`, dan `/community` sudah `200 OK` dari `edgeone-pages`, sementara blocker utama menyempit ke `https://thechoosentalks.org` yang masih reset saat handshake/koneksi.
